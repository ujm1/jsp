import express from 'express';
/**
 * Handles TSConfig file creation requests
 */
export default function (request: express.Request, response: express.Response, next: express.NextFunction): Promise<void>;
